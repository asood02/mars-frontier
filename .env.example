# Online multiplayer relay (self-hosted WebSocket server: `npm run server`).
VITE_WS_URL=ws://localhost:8787

# Optional hosted backend (Supabase) — not required for the WS relay above.
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
